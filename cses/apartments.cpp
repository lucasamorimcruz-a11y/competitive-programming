#include <bits/stdc++.h>
using namespace std;
#define int int64_t

int32_t main()
{
    int n, m, k;
    cin >> n >> m >> k;
    vector<int> a (n);
    for (auto &x : a) cin >> x;
    vector<int> b (m);
    for (auto &x : b) cin >> x;
    sort (a.begin(), a.end());
    int l = 0;
    int r = a.end();
    for (int l = 0; l < a.end();)
    return 0;
}
