#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    while (t--)
    {
        int x, y;
        cin >> x >> y;
        vector<vector<int>> spiral(maxn, vector<int>(maxn));
        for (int i = 0; i < maxn; i++)
        {
            for (int j = 0; j < maxn; j++)
            {
                if (y < x)
                {
                    
                }
            }
        }
        int num = spiral[x][y];
        cout << num << '\n';
    }
}