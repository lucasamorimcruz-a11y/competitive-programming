#include <bits/stdc++.h>
using namespace std;
#define int int64_t

int32_t main(){
    int t; cin >> t;
    while (t--){
        int n, a, b; cin >> n >> a >> b;
        vector<int> p (n), q (n);
        
    }
    return 0;
}

[1,3,4,2,5] -> outra permutação. 
[1,2,3,4,5] -> uma permutação.