#include <bits/stdc++.h>
using namespace std;
#define int int64_t

int32_t main(){
    int t; cin >> t;
    while (t--){
        int max_bomb_timer, bomb_timer, tools;
        cin >> max_bomb_timer >> bomb_timer >> tools;
        vector <int> n_tools (tools);
        for (auto &x : n_tools) cin >> x;                
    }    
    return 0;
}
