#include <bits/stdc++.h>
using namespace std;
#define int int64_t

int32_t main(){
    int n, k; cin >> n >> k;
    vector<int> a (n);
    for (auto &x : a ) cin >> x;    
    
    int ans =0; 
    cout << ans << '\n';
    return 0;
}
